definitions:
  signinModelReqPOST:
    type: "object"
    properties:
      email:
        type: string
        description: "User's email."
        example: test@gmail.com
      password:
        type: string
        description: "User's password."
        format: "password"
        example : oldboy
    required:
      - email
      - password
